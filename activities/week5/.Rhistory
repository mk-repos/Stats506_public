geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge2(width = 0.5)) +
theme_bw() +
xlab('% of Homes') +
scale_color_manual(
values = c('darkblue', 'red3')
)
# Remove comments to plot and save
#ggsave(p1, 'w4_p2_q1_plot.png')
p1
cap = paste("**Figure 1.** *Proportion of homes with internet access in each",
"Census Division.*")
# Use ggplot to produce a figure `p1` matching what is shown using the
#  data above.
p1 = plot_data %>%
filter(rurality != "diff") %>%
ggplot( mapping = aes(x = est, y = division),
color = rurality, shape = rurality) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(width = 0.5)) +
theme_bw() +
xlab('% of Homes') +
scale_color_manual(
values = c('darkblue', 'red3')
)
# Remove comments to plot and save
#ggsave(p1, 'w4_p2_q1_plot.png')
p1
cap = paste("**Figure 1.** *Proportion of homes with internet access in each",
"Census Division.*")
# Use ggplot to produce a figure `p1` matching what is shown using the
#  data above.
p1 = plot_data %>%
filter(rurality != "diff") %>%
ggplot( mapping = aes(x = est, y = division),
color = rurality, shape = rurality) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(hight = 0.5)) +
theme_bw() +
xlab('% of Homes') +
scale_color_manual(
values = c('darkblue', 'red3')
)
cap = paste("**Figure 1.** *Proportion of homes with internet access in each",
"Census Division.*")
# Use ggplot to produce a figure `p1` matching what is shown using the
#  data above.
p1 = plot_data %>%
filter(rurality != "diff") %>%
ggplot( mapping = aes(x = est, y = division),
color = rurality, shape = rurality) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(height = 0.5)) +
theme_bw() +
xlab('% of Homes') +
scale_color_manual(
values = c('darkblue', 'red3')
)
cap = paste("**Figure 1.** *Proportion of homes with internet access in each",
"Census Division.*")
# Use ggplot to produce a figure `p1` matching what is shown using the
#  data above.
p1 = plot_data %>%
filter(rurality != "diff") %>%
ggplot( mapping = aes(x = est, y = division),
color = rurality, shape = rurality) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(width = 0.5)) +
theme_bw() +
xlab('% of Homes') +
scale_color_manual(
values = c('darkblue', 'red3')
)
# Remove comments to plot and save
#ggsave(p1, 'w4_p2_q1_plot.png')
p1
cap = paste("**Figure 1.** *Proportion of homes with internet access in each",
"Census Division.*")
# Use ggplot to produce a figure `p1` matching what is shown using the
#  data above.
p1 = plot_data %>%
filter(rurality != "diff") %>%
ggplot( mapping = aes(x = est, y = division),
color = rurality, shape = rurality) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge2(width = 0.5)) +
theme_bw() +
xlab('% of Homes') +
scale_color_manual(
values = c('darkblue', 'red3')
)
# Remove comments to plot and save
#ggsave(p1, 'w4_p2_q1_plot.png')
p1
cap = paste("**Figure 1.** *Proportion of homes with internet access in each",
"Census Division.*")
# Use ggplot to produce a figure `p1` matching what is shown using the
#  data above.
p1 = plot_data %>%
filter(rurality != "diff") %>%
ggplot( mapping = aes(x = est, y = division),
color = rurality, shape = rurality) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge2(width = 0.7)) +
theme_bw() +
xlab('% of Homes') +
scale_color_manual(
values = c('darkblue', 'red3')
)
# Remove comments to plot and save
#ggsave(p1, 'w4_p2_q1_plot.png')
p1
cap = paste("**Figure 1.** *Proportion of homes with internet access in each",
"Census Division.*")
# Use ggplot to produce a figure `p1` matching what is shown using the
#  data above.
p1 = plot_data %>%
filter(rurality != "diff") %>%
ggplot( mapping = aes(x = est, y = division),
color = rurality, shape = rurality) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge2(width = 1)) +
theme_bw() +
xlab('% of Homes') +
scale_color_manual(
values = c('darkblue', 'red3')
)
# Remove comments to plot and save
#ggsave(p1, 'w4_p2_q1_plot.png')
p1
cap = paste("**Figure 1.** *Proportion of homes with internet access in each",
"Census Division.*")
# Use ggplot to produce a figure `p1` matching what is shown using the
#  data above.
p1 = plot_data %>%
filter(rurality != "diff") %>%
ggplot( mapping = aes(x = est, y = division),
color = rurality, shape = rurality) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge2(width = 0.5)) +
theme_bw() +
xlab('% of Homes') +
scale_color_manual(
values = c('darkblue', 'red3')
)
# Remove comments to plot and save
#ggsave(p1, 'w4_p2_q1_plot.png')
p1
?position_dodge2
?position_dodge
cap = paste("**Figure 1.** *Proportion of homes with internet access in each",
"Census Division.*")
# Use ggplot to produce a figure `p1` matching what is shown using the
#  data above.
p1 = plot_data %>%
filter(rurality != "diff") %>%
ggplot( mapping = aes(x = est, y = division),
color = rurality, shape = rurality) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge1(width = 0.5),
height = 0.75,
alpha = 0.75) +
theme_bw() +
xlab('% of Homes') +
scale_color_manual(
values = c('darkblue', 'red3')
)
cap = paste("**Figure 1.** *Proportion of homes with internet access in each",
"Census Division.*")
# Use ggplot to produce a figure `p1` matching what is shown using the
#  data above.
p1 = plot_data %>%
filter(rurality != "diff") %>%
ggplot( mapping = aes(x = est, y = division),
color = rurality, shape = rurality) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(width = 0.5),
height = 0.75,
alpha = 0.75) +
theme_bw() +
xlab('% of Homes') +
scale_color_manual(
values = c('darkblue', 'red3')
)
# Remove comments to plot and save
#ggsave(p1, 'w4_p2_q1_plot.png')
p1
cap = paste("**Figure 1.** *Proportion of homes with internet access in each",
"Census Division.*")
# Use ggplot to produce a figure `p1` matching what is shown using the
#  data above.
p1 = plot_data %>%
filter(rurality != "diff") %>%
ggplot( mapping = aes(x = est, y = division, color = rurality)) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(width = 0.5),
height = 0.75,
alpha = 0.75) +
theme_bw() +
xlab('% of Homes') +
scale_color_manual(
values = c('darkblue', 'red3')
)
# Remove comments to plot and save
#ggsave(p1, 'w4_p2_q1_plot.png')
p1
cap = paste("**Figure 1.** *Proportion of homes with internet access in each",
"Census Division.*")
# Use ggplot to produce a figure `p1` matching what is shown using the
#  data above.
p1 = plot_data %>%
filter(rurality != "diff") %>%
ggplot( mapping = aes(x = est, y = division, color = rurality)) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(width = 0.5),
height = 0.75,
alpha = 0.75) +
theme_bw() +
xlab('Proportion of homes with internet access.') +
ylab('division') +
scale_color_manual(
values = c('darkblue', 'red3')
)
# Remove comments to plot and save
#ggsave(p1, 'w4_p2_q1_plot.png')
p1
cap = paste("**Figure 1.** *Proportion of homes with internet access in each",
"Census Division.*")
# Use ggplot to produce a figure `p1` matching what is shown using the
#  data above.
p1 = plot_data %>%
filter(rurality != "diff") %>%
ggplot( mapping = aes(x = est, y = division, color = rurality)) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(width = 0.5),
height = 0.75,
alpha = 0.75) +
theme_bw() +
xlab('Proportion of homes with internet access.') +
ylab('division') +
scale_color_manual(
values = c('red3','darkblue')
)
# Remove comments to plot and save
#ggsave(p1, 'w4_p2_q1_plot.png')
p1
cap = paste("**Figure 2.** *Difference (urban less rural) in proportion of",
"homes with internet access in each Census Division.*")
# Use ggplot to produce a figure `p2` matching what is shown using the
#  data above.
p2 = plot_data %>%
filter(rurality = "diff") %>%
ggplot( mapping = aes(x = est, y = division)) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(width = 0.5),
height = 0.75,
alpha = 0.75) +
theme_bw() +
xlab('Proportion of homes with internet access.') +
ylab('division') +
scale_color_manual(
values = c('red3','darkblue')
)
cap = paste("**Figure 2.** *Difference (urban less rural) in proportion of",
"homes with internet access in each Census Division.*")
# Use ggplot to produce a figure `p2` matching what is shown using the
#  data above.
p2 = plot_data %>%
filter(rurality == "diff") %>%
ggplot( mapping = aes(x = est, y = division)) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(width = 0.5),
height = 0.75,
alpha = 0.75) +
theme_bw() +
xlab('Proportion of homes with internet access.') +
ylab('division') +
scale_color_manual(
values = c('red3','darkblue')
)
# Remove comments to plot and save
#ggsave(p2, 'w4_p2_q2_plot.png')
#p2
# Remove comments to plot and save
#ggsave(p2, 'w4_p2_q2_plot.png')
p2
cap = paste("**Figure 2.** *Difference (urban less rural) in proportion of",
"homes with internet access in each Census Division.*")
# Use ggplot to produce a figure `p2` matching what is shown using the
#  data above.
p2 = plot_data %>%
filter(rurality == "diff") %>%
ggplot( mapping = aes(x = est, y = division)) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(width = 0.5),
height = 0.75,
alpha = 0.75) +
geom_vline(aes(x = 0)) +
theme_bw() +
xlab('Proportion of homes with internet access.') +
ylab('division') +
scale_color_manual(
values = c('red3','darkblue')
)
# Remove comments to plot and save
#ggsave(p2, 'w4_p2_q2_plot.png')
p2
cap = paste("**Figure 2.** *Difference (urban less rural) in proportion of",
"homes with internet access in each Census Division.*")
# Use ggplot to produce a figure `p2` matching what is shown using the
#  data above.
p2 = plot_data %>%
filter(rurality == "diff") %>%
ggplot( mapping = aes(x = est, y = division)) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(width = 0.5),
height = 0.75,
alpha = 0.75) +
geom_vline(aes(xintercept = 0)) +
theme_bw() +
xlab('Proportion of homes with internet access.') +
ylab('division') +
scale_color_manual(
values = c('red3','darkblue')
)
# Remove comments to plot and save
#ggsave(p2, 'w4_p2_q2_plot.png')
p2
?geom_vline
cap = paste("**Figure 2.** *Difference (urban less rural) in proportion of",
"homes with internet access in each Census Division.*")
# Use ggplot to produce a figure `p2` matching what is shown using the
#  data above.
p2 = plot_data %>%
filter(rurality == "diff") %>%
ggplot( mapping = aes(x = est, y = division)) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(width = 0.5),
height = 0.75,
alpha = 0.75) +
geom_vline(xintercept = 0) +
theme_bw() +
xlab('Proportion of homes with internet access.') +
ylab('division') +
scale_color_manual(
values = c('red3','darkblue')
)
# Remove comments to plot and save
#ggsave(p2, 'w4_p2_q2_plot.png')
p2
cap = paste("**Figure 2.** *Difference (urban less rural) in proportion of",
"homes with internet access in each Census Division.*")
# Use ggplot to produce a figure `p2` matching what is shown using the
#  data above.
p2 = plot_data %>%
filter(rurality == "diff") %>%
ggplot( mapping = aes(x = est, y = division)) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(width = 0.5),
height = 0.75,
alpha = 0.75) +
geom_vline(xintercept = 0, linetype = "dashed") +
theme_bw() +
xlab('Proportion of homes with internet access.') +
ylab('division') +
scale_color_manual(
values = c('red3','darkblue')
)
# Remove comments to plot and save
#ggsave(p2, 'w4_p2_q2_plot.png')
p2
cap = paste("**Figure 2.** *Difference (urban less rural) in proportion of",
"homes with internet access in each Census Division.*")
# Use ggplot to produce a figure `p2` matching what is shown using the
#  data above.
p2 = plot_data %>%
filter(rurality == "diff") %>%
ggplot( mapping = aes(x = est, y = division)) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(width = 0.5),
height = 0.75,
alpha = 0.75) +
geom_vline(xintercept = 0, linetype = "dashed") +
theme_bw() +
xlab('Difference in proportion of homes with internet access.') +
ylab('division')
# Remove comments to plot and save
#ggsave(p2, 'w4_p2_q2_plot.png')
p2
setwd("~/Dropbox (University of Michigan)/GitHub/stats506/week4_activity")
knitr::opts_chunk$set(echo = TRUE)
# libraries
library(tidyverse)
# read in summary results
q2_df = readr::read_delim("./w4_p2_RECS2015_internet.csv", delim = ",")
# reshape for plotting
plot_data = q2_df %>%
pivot_longer(cols = starts_with('p_int'), names_prefix = 'p_int',
names_to = 'var') %>%
mutate( rurality = stringr::str_replace_all(var, '_.*', ''),
type = ifelse( grepl('_', var),
stringr::str_replace_all(var, '.*_', ''),
'est'
)
) %>%
pivot_wider(id_cols = c('division', 'rurality'),
names_from = type,
values_from = value
)
plot_data
cap = paste("**Figure 1.** *Proportion of homes with internet access in each",
"Census Division.*")
# Use ggplot to produce a figure `p1` matching what is shown using the
#  data above.
p1 = plot_data %>%
filter(rurality != "diff") %>%
ggplot( mapping = aes(x = est, y = division, color = rurality)) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(width = 0.5),
height = 0.75,
alpha = 0.75) +
theme_bw() +
xlab('Proportion of homes with internet access.') +
ylab('division') +
scale_color_manual(
values = c('red3','darkblue')
)
# Remove comments to plot and save
ggsave(p1, 'w4_p2_q1_plot.png')
cap = paste("**Figure 1.** *Proportion of homes with internet access in each",
"Census Division.*")
# Use ggplot to produce a figure `p1` matching what is shown using the
#  data above.
p1 = plot_data %>%
filter(rurality != "diff") %>%
ggplot( mapping = aes(x = est, y = division, color = rurality)) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(width = 0.5),
height = 0.75,
alpha = 0.75) +
theme_bw() +
xlab('Proportion of homes with internet access.') +
ylab('division') +
scale_color_manual(
values = c('red3','darkblue')
)
# Remove comments to plot and save
ggsave(p1, 'w4_p2_q1_plot.png', device="png")
cap = paste("**Figure 1.** *Proportion of homes with internet access in each",
"Census Division.*")
# Use ggplot to produce a figure `p1` matching what is shown using the
#  data above.
p1 = plot_data %>%
filter(rurality != "diff") %>%
ggplot( mapping = aes(x = est, y = division, color = rurality)) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(width = 0.5),
height = 0.75,
alpha = 0.75) +
theme_bw() +
xlab('Proportion of homes with internet access.') +
ylab('division') +
scale_color_manual(
values = c('red3','darkblue')
)
# Remove comments to plot and save
ggsave(p1, filename = 'w4_p2_q1_plot.png', device="png")
p1
cap = paste("**Figure 2.** *Difference (urban less rural) in proportion of",
"homes with internet access in each Census Division.*")
# Use ggplot to produce a figure `p2` matching what is shown using the
#  data above.
p2 = plot_data %>%
filter(rurality == "diff") %>%
ggplot( mapping = aes(x = est, y = division)) +
geom_point(position = position_dodge2(width = 0.5)) +
geom_errorbarh( aes(xmin = lwr, xmax = upr),
position = position_dodge(width = 0.5),
height = 0.75,
alpha = 0.75) +
geom_vline(xintercept = 0, linetype = "dashed") +
theme_bw() +
xlab('Difference in proportion of homes with internet access.') +
ylab('division')
# Remove comments to plot and save
ggsave(p2, filename = 'w4_p2_q2_plot.png', device="png")
p2
setwd("~/Documents/Stats506_public/activities/week5")
